# Installation

```
- git clone git@github.com:Sunwarul/interview-app-1.git
- cd interview-app-1
- composer install
- npm install && npm run dev
- php artisan migrate --seed
- php artisan serve to serve the app locally
```
